// src/lib/auth.ts (client-side)
import { createAuthClient } from 'better-auth/react';

// Initialize the auth client
export const auth = createAuthClient({
  // Configuration will come from environment variables
});